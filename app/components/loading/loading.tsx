const Loading = () => {
    return (
        <div className="flex items-center justify-center w-full h-screen bg-inherit">
            <p>Loading...</p>
        </div>
    )
}

export default Loading
